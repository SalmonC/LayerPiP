# dmMiniPlayer 网络字幕修改版

这是基于 dmMiniPlayer Edge v0.6.60 的非商业修改版。它保留本地 SRT/ASS 导入，并在同一字幕菜单中增加“添加网络字幕”。网络字幕会直接进入插件自身的字幕管理器，因此能显示在系统级画中画中。

## 安装

1. 在 Edge 打开 `edge://extensions/`，打开“开发人员模式”。
2. 暂时停用商店安装的“弹幕画中画播放器”，避免两个版本同时注入页面。
3. 点击“加载解压缩的扩展”，选择本项目的 `dist` 文件夹。
4. 确认扩展名称显示为“弹幕画中画播放器（网络字幕版）”，然后刷新已经打开的 B 站页面。

若要回退，删除或停用网络字幕版，再重新启用商店版本即可。商店版数据不会被本项目删除。

## 使用

1. 打开目标视频 A，并启动插件的系统级画中画。
2. 鼠标移到画中画底部的字幕按钮，点击“添加网络字幕”。
3. 粘贴来源 B 的视频链接：
   - 包含 `p`：严格读取该分P，例如 `https://www.bilibili.com/video/BV15UREYsEN8/?p=57`。
   - 不含 `p`：先显示分P选择器，不会默认猜测第1P。
4. 点击“解析链接”，选择字幕轨道，再点击“加载网络字幕”。

也可直接粘贴 SRT、ASS 或 B 站字幕 JSON 的 HTTP/HTTPS 链接。直接链接通过扩展后台读取，不受页面 CORS 限制。

“时间偏移（秒）”沿用字幕桥脚本的语义：`B时间 = A时间 + 偏移`。正数让字幕更早显示，负数让字幕更晚显示。

## 限制

- B 站部分字幕要求登录。出现登录提示时，请先在当前 Edge 配置中登录 B 站并刷新页面。
- 修改版不会跟随 Edge 商店自动更新；升级上游版本时需要重新合并本功能。
- 网络字幕单文件上限为 10 MB，仅支持 SRT、ASS 和 B 站 `body` JSON 格式。

## 来源与许可

- 上游项目：`apades/dmMiniPlayer`
- 基线：Edge v0.6.60 / Git tag `v0.6.60` / commit `b02c21e`
- 上游仓库的根 `LICENSE` 为 CC BY-NC 4.0。本修改版保留作者、主页和完整许可证，仅用于非商业用途。
