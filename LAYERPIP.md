# 叠映 LayerPiP

LayerPiP 是一个与旧插件彻底隔离的新项目，基于 `apades/dmMiniPlayer v0.6.60` 的非商业自用分支开发，专注于 macOS Edge 中的学习视频小窗。

## 核心范围

- Bilibili 普通视频、分 P、番剧页面与直播
- 通用 HTML5 视频后备支持
- 增强小窗：Document Picture-in-Picture，保留完整交互控件
- 原生小窗：把视频、弹幕和字幕合成后交给 Edge 原生画中画
- Bilibili 原生弹幕、显示开关与时间偏移
- 原生字幕、本地 SRT/ASS，以及 Bilibili 视频链接或直接字幕链接
- 来源链接含 `p` 时默认该分 P；不含 `p` 时默认 P1，允许手动改选
- 播放、快进快退、音量、倍速、上一/下一 P、字幕与弹幕快捷键

## 与稳定旧插件共存

稳定旧插件保留在相邻的 `FloatCaption` 工作树和 Git 标签 `custom-v0.7.1`。LayerPiP 使用独立工作树、固定扩展 ID、配置命名空间、版本和图标。在运行时命名空间隔离完成并通过验收前，不要在同一个 Edge 配置文件中同时启用两个插件。

## 安装与日常更新

固定加载位置是本项目根目录下的 `dist`，不要加载临时构建目录，也不要移动已加载的目录。

1. 首次使用：Edge 打开 `edge://extensions`，启用“开发人员模式”。
2. 选择“加载解压缩的扩展”，选择本项目的 `dist` 文件夹。
3. 确认名称“叠映 LayerPiP”，ID `jnonlboihmjeahenhlbkjdijicakfnjj`。旧 FloatCaption 保留但停用，避免同时干预同一播放器。
4. 后续更新交付到同一目录后：关闭现有小窗，在扩展页点击 LayerPiP 的“重新加载”，然后刷新 B站页面。
5. 原生功能、界面与实际播放体验由用户验收，构建通过不代表全部功能已验收。

开发交付：已有依赖时运行 `pnpm build`（也可直接运行 `node scripts/build-local.mjs`）。脚本在 `.delivery` 中构建，核对扩展 ID、版本和必要文件，源文件在构建期间变化则停止。成功后保留旧产物并替换固定 `dist`；失败不交付半成品。不同内容的交付必须递增 `package.json` 版本。

本地 `.delivery/receipt-*.json` 保存源码指纹、产物哈希及回退位置；`.delivery/rollback-*` 保留此前产物，不等于已经过用户验收的稳定版。首次配置依赖才需要 `pnpm install --frozen-lockfile`，普通更新不重装依赖。

## 本地 AI 字幕（首版）

先打开小窗，在插件设置的字幕页启用“本地 AI 字幕”，保持视频 1 倍速播放后点击“开始识别”。增强小窗也可在字幕来源菜单中开始/停止。开启总开关只显示功能入口，不会自动加载模型。关闭总开关会停止识别、释放资源并隐藏普通播放器中的入口。

首版随插件携带多语言 Whisper Base q8 模型和浏览器运行时，无需安装其他软件、下载单独模型或提供 API Key。音频在本机处理，生成原语言字幕；此功能不包含自动翻译。按约 8 秒分段，识别有延迟，建议开启“历史字幕”跟看；关闭历史时已过期的片段不会延长显示。跨段句子可能截断，识别文字需要核对。

首版用于 1 倍速视频。切换视频、跳转、关闭小窗或修改倍速会停止识别；回到支持的状态后手动重新开始。浏览器无法采集音频、模型失败或处理持续落后时只停止 AI；视频播放不随之暂停。中文识别质量、延迟和实际 B 站采集效果由用户验收，不保证所有设备实时运行。

给开发者：模型二进制不直接提交 Git。首次构建先运行 `node scripts/prepare-ai-model.mjs`，从固定版本下载并核对 SHA-256；之后构建离线读取已校验资源并将其包含在 `dist`。最终用户不需要执行此步骤。架构与未来模型管理接口见 `docs/local-ai-subtitles.md`。

## 已主动精简

- 移除自定义弹幕发送、本地/网络弹幕导入入口
- 移除网页替换播放器入口、更新日志气泡、快速隐藏等非核心命令
- YouTube、Netflix、Twitch 等不再使用专属适配，统一走通用 HTML5 后备路径
- 设置面板只显示播放、字幕、弹幕和快捷键相关项目
- 隐藏配置会自动归一到安全值，尤其是 Document PiP、`replaceVideoEl` 和核心控制按钮

## 许可与署名

保留上游署名并按仓库根目录的 CC BY-NC 4.0 许可处理，仅用于非商业用途。上游项目：<https://github.com/apades/dmMiniPlayer>。
